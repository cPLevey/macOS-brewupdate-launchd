# macOS-brewupdate-launchd
macOS version of a cron job to automatically update homebrew installations via launchd.

Auto install:
``` /bin/sh <(curl -s https://cdn.rawgit.com/cPLevey/macOS-brewupdate-launchd/master/installer.sh) ```